# AI SLM Training - all dependencies (data prep, training, demo)
# Used for both local install and Docker. Install: pip install -r requirements.txt

# ---------------------------------------------------------------------------
# Data preparation (PDF, CSV, chunking)
# ---------------------------------------------------------------------------
pdfplumber>=0.10.0
PyPDF2>=3.0.0

# ---------------------------------------------------------------------------
# Core ML / Training and inference
# ---------------------------------------------------------------------------
torch>=2.1.0
transformers>=4.36.0
datasets>=2.14.0
accelerate>=0.25.0
peft>=0.7.0
trl>=0.7.4
bitsandbytes>=0.41.0
# unsloth requires CUDA — installed optionally in Dockerfile (pip install unsloth || true)

# ONNX Runtime — CPU inference acceleration via optimum (optional but recommended)
optimum>=1.16.0
onnxruntime>=1.17.0

# Hugging Face / model loading (often pulled by transformers but required explicitly)
safetensors>=0.4.0
huggingface-hub>=0.20.0
tokenizers>=0.15.0
regex>=2023.0

# ---------------------------------------------------------------------------
# Web UI for Q&A demo
# ---------------------------------------------------------------------------
gradio>=4.0.0

# ---------------------------------------------------------------------------
# Utilities (used by transformers, datasets, gradio, etc.)
# ---------------------------------------------------------------------------
numpy>=1.24.0
requests>=2.28.0
tqdm>=4.66.0
filelock>=3.12.0
fsspec>=2023.0
pyyaml>=6.0
Pillow>=9.0.0
charset-normalizer>=3.0.0
