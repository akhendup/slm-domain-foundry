# SLM Domain Foundry — full install (data + training + inference)
# For lighter installs see requirements-core.txt, requirements-train.txt, requirements-inference.txt

-r requirements-train.txt
optimum>=1.16.0
onnxruntime>=1.17.0
gradio>=4.0.0
Pillow>=9.0.0
